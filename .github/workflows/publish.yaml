name: Publish Helm Chart

on:
  release:
    types:
      - created

jobs:
  publish:
    uses: your-org/github-actions/.github/workflows/publish-helm-chart.yml@main
    permissions:
      contents: write
