image:
  repository: 
  tag: latest

deployment:
  replicas: 1
  port:  
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

ingress:
  enabled: false
  hosts:
    - www.tylerpitcher.com
    - tylerpitcher.com
  path: /
  tlsSecretName: tls-secret
  httpsRedirectMiddleware: https-redirect
