name: Noteable Changes

on:
  release:
    types:
      - created

jobs:
  update-notes:
    uses: tylerpitcher/ci-cd/.github/workflows/noteable.yaml@main
    secrets:
      GIT_PAT: ${{ secrets.GIT_PAT }}
